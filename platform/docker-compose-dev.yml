version: "3.9"

services:
  db:
    image: postgres:13.1
    volumes:
      - ./data/db:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=flexadf
      - POSTGRES_USER=flexadf
      - POSTGRES_PASSWORD=ASDVIfsaHU23HfFAS8dpl
    ports:
      - "5432:5432"
